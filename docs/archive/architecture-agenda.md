# 📋 Архитектура Агенды v2.0

> **Статус**: ✅ Реализовано  
> **Целевая версия**: v3.0.4  
> **Создан**: 2026-02-03  
> **Соответствие**: [CODE_STANDARDS.md](CODE_STANDARDS.md), [CODE_STANDARDS-RU.md](CODE_STANDARDS-RU.md)  
> **Связанные документы**: [architecture-database-view.md](architecture-database-view.md)

---

## 📊 Обзор проекта

### Текущее состояние
Агенда в Projects Plus представляет собой список задач и событий, сгруппированных по датам. Текущая реализация имеет ограниченные возможности кастомизации.

### Цели переработки
1. **Альтернативные режимы** — переключение между агендами в настройках проекта
2. **Синхронизация фильтров** — фильтры календаря применяются к стандартной агенде
3. **Конструктор Агенды** — полностью настраиваемые списки с пользовательскими фильтрами

---

## 📏 Стандарты разработки

### Обязательные требования

Все компоненты Agenda 2.0 ДОЛЖНЫ соответствовать стандартам плагина:

| Требование | Стандарт | Документация |
|------------|----------|--------------|
| **Единицы измерения** | Только `rem` (не `px`) | [convert-px-to-rem.ps1](../scripts/convert-px-to-rem.ps1) |
| **CSS переменные** | Использовать Obsidian токены | `--background-primary`, `--text-normal` и т.д. |
| **TypeScript** | Strict mode, no unjustified `any` | [CODE_STANDARDS.md](CODE_STANDARDS.md) |
| **Promise handling** | Все промисы обработаны | `void`, `.catch()`, `await` |
| **localStorage** | Только `App.loadLocalStorage()` | Obsidian API |
| **console.log** | Запрещено, только `console.debug()` | Production code |

### Конвертация единиц

```
Формула: rem = px / 16

Примеры:
- 44px → 2.75rem (минимальный touch target)
- 16px → 1rem (базовый размер)
- 8px → 0.5rem (отступ)
- 4px → 0.25rem (мелкий отступ)
- 1px → 0.0625rem (граница)
```

### CSS Токены Obsidian

```css
/* ✅ ПРАВИЛЬНО — используем переменные Obsidian */
.agenda-item {
  background-color: var(--background-primary);
  color: var(--text-normal);
  border: 0.0625rem solid var(--background-modifier-border);
  border-radius: var(--radius-m);
  padding: var(--size-4-2);
  gap: 0.5rem;
}

/* ❌ НЕПРАВИЛЬНО — жёсткие значения */
.agenda-item {
  background-color: #ffffff;
  color: #333333;
  border: 1px solid #e0e0e0;
  border-radius: 4px;
  padding: 8px;
}
```

---

## 🏗️ Архитектура системы

### Компонентная структура

```
src/ui/views/Calendar/
├── agenda/
│   ├── AgendaView.svelte              # Контейнер агенды (переключатель режимов)
│   ├── AgendaStandard.svelte          # Стандартная агенда (с фильтрами календаря)
│   ├── AgendaCustom.svelte            # Конструктор (пользовательские списки)
│   ├── AgendaSettings.svelte          # Модал настройки кастомной агенды
│   ├── AgendaListItem.svelte          # Элемент списка агенды
│   ├── AgendaListBuilder.svelte       # Конструктор списка
│   ├── AgendaFilterEditor.svelte      # Редактор условий фильтрации
│   ├── AgendaIconPicker.svelte        # Выбор иконок
│   └── types.ts                        # Типы и интерфейсы
├── components/
│   └── ...
└── CalendarView.svelte
```

---

## 📐 Типы и интерфейсы

### Конфигурация Агенды (ProjectDefinition)

```typescript
// В settings/v3/settings.ts
export type AgendaMode = 'standard' | 'custom';

export type AgendaConfig = {
  // Режим агенды
  readonly mode: AgendaMode;
  
  // Настройки стандартной агенды
  readonly standard?: {
    readonly inheritCalendarFilters: boolean;  // Наследовать фильтры календаря
  };
  
  // Настройки кастомной агенды
  readonly custom?: {
    readonly lists: AgendaCustomList[];
  };
};

export type ProjectDefinition<ViewDef> = {
  // ... существующие поля
  readonly agenda?: AgendaConfig;  // Конфигурация агенды
};
```

### Кастомный список Агенды

```typescript
export type AgendaCustomList = {
  readonly id: string;              // Уникальный ID списка
  readonly name: string;            // Пользовательское название
  readonly icon: AgendaIcon;        // Иконка списка
  readonly filters: AgendaFilter[]; // Условия фильтрации
  readonly order: number;           // Порядок отображения
  readonly collapsed?: boolean;     // Свёрнут по умолчанию
  readonly color?: string;          // Цвет акцента (опционально)
};

export type AgendaIcon = {
  readonly type: 'lucide' | 'emoji' | 'obsidian-icon' | 'plugin-icon';
  readonly value: string;           // Имя иконки или emoji
};

export type AgendaFilter = {
  readonly id: string;
  readonly field: string;           // Имя поля для фильтрации
  readonly operator: FilterOperator;
  readonly value: FilterValue;
  readonly conjunction: 'AND' | 'OR'; // Связь с предыдущим условием
};

export type FilterOperator = 
  | 'equals'
  | 'not_equals'
  | 'contains'
  | 'not_contains'
  | 'starts_with'
  | 'ends_with'
  | 'is_empty'
  | 'is_not_empty'
  | 'greater_than'
  | 'less_than'
  | 'greater_or_equal'
  | 'less_or_equal'
  | 'is_today'
  | 'is_this_week'
  | 'is_overdue'
  | 'is_upcoming';

export type FilterValue = string | number | boolean | string[] | null;
```

---

## 🔄 Режимы работы

### 1. Стандартная Агенда (Standard)

**Принцип**: Наследует фильтры обзора календаря

```
┌─────────────────────────────────────┐
│         Календарь (Calendar)        │
│   [Фильтры: status, priority...]    │
│                 │                   │
│                 ▼                   │
│ ┌─────────────────────────────────┐ │
│ │      Стандартная Агенда         │ │
│ │   (те же фильтры применены)     │ │
│ │                                 │ │
│ │   📅 Сегодня                    │ │
│ │   ├─ Task 1 (filtered)          │ │
│ │   └─ Task 2 (filtered)          │ │
│ │                                 │ │
│ │   📅 Завтра                     │ │
│ │   └─ Task 3 (filtered)          │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**Реализация**:
```typescript
// AgendaStandard.svelte
<script lang="ts">
  import { calendarFilters } from '../stores/calendarFilters';
  
  export let records: DataRecord[];
  export let dateField: DataField;
  
  // Применяем фильтры календаря к записям агенды
  $: filteredRecords = applyCalendarFilters(records, $calendarFilters);
  
  // Группируем по датам
  $: groupedByDate = groupRecordsByDate(filteredRecords, dateField);
</script>
```

### 2. Конструктор Агенды (Custom)

**Принцип**: Полностью независимые пользовательские списки

```
┌─────────────────────────────────────┐
│        Кастомная Агенда            │
│   (независимые фильтры списков)    │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 🔥 Срочные задачи               │ │
│ │    [priority = high AND         │ │
│ │     due <= today]               │ │
│ │   ├─ Urgent task 1              │ │
│ │   └─ Urgent task 2              │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 📝 В работе                     │ │
│ │    [status = "in-progress"]     │ │
│ │   └─ Task in progress           │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ ⏰ На этой неделе               │ │
│ │    [due is_this_week]           │ │
│ │   ├─ Task 1                     │ │
│ │   ├─ Task 2                     │ │
│ │   └─ Task 3                     │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [+ Добавить список]                 │
└─────────────────────────────────────┘
```

**Важно**: Фильтры кастомной агенды НЕ влияют на остальной обзор календаря.

---

## 🎨 UI/UX Компоненты

### Модал настройки Агенды

```
┌─────────────────────────────────────────────┐
│  ⚙️ Настройка Агенды                    [X] │
├─────────────────────────────────────────────┤
│                                             │
│  Режим агенды:                              │
│  ┌─────────────────────────────────────────┐│
│  │ ○ Стандартная (фильтры календаря)       ││
│  │ ● Конструктор (свои списки)             ││
│  └─────────────────────────────────────────┘│
│                                             │
│  ─────────────────────────────────────────  │
│                                             │
│  📋 Списки:                                 │
│                                             │
│  ┌─────────────────────────────────────────┐│
│  │ 🔥 Срочные задачи              [⋮] [↕] ││
│  │    priority = high AND due <= today     ││
│  └─────────────────────────────────────────┘│
│                                             │
│  ┌─────────────────────────────────────────┐│
│  │ 📝 В работе                    [⋮] [↕] ││
│  │    status = "in-progress"               ││
│  └─────────────────────────────────────────┘│
│                                             │
│  [+ Добавить список]                        │
│                                             │
├─────────────────────────────────────────────┤
│                            [Отмена] [Сохр.] │
└─────────────────────────────────────────────┘
```

### Редактор списка

```
┌─────────────────────────────────────────────┐
│  📝 Редактирование списка                   │
├─────────────────────────────────────────────┤
│                                             │
│  Название: [Срочные задачи_____________]    │
│                                             │
│  Иконка:   [🔥] [Выбрать...]               │
│                                             │
│  ─────────────────────────────────────────  │
│                                             │
│  Условия фильтрации:                        │
│                                             │
│  ┌─────────────────────────────────────────┐│
│  │ [priority ▼] [равно ▼] [high     ▼] [×]││
│  └─────────────────────────────────────────┘│
│           AND                               │
│  ┌─────────────────────────────────────────┐│
│  │ [due      ▼] [<=     ▼] [сегодня ▼] [×]││
│  └─────────────────────────────────────────┘│
│                                             │
│  [+ Добавить условие]                       │
│                                             │
│  ─────────────────────────────────────────  │
│                                             │
│  [◯ AND между условиями]                   │
│  [● OR между условиями]                    │
│                                             │
├─────────────────────────────────────────────┤
│                            [Отмена] [Сохр.] │
└─────────────────────────────────────────────┘
```

### Выбор иконок

```
┌─────────────────────────────────────────────┐
│  🎨 Выбор иконки                        [X] │
├─────────────────────────────────────────────┤
│                                             │
│  [🔍 Поиск...________________________]     │
│                                             │
│  Lucide Icons:                              │
│  ┌─────────────────────────────────────────┐│
│  │ 📋 📌 📝 ✅ ⏰ 🔥 ⭐ 💡 🎯 📅 ││
│  │ 📊 📈 🗂️ 📁 🏷️ 🔔 💬 📧 🔗 ⚙️ ││
│  └─────────────────────────────────────────┘│
│                                             │
│  Эмодзи:                                    │
│  ┌─────────────────────────────────────────┐│
│  │ 😀 😎 🚀 💪 🎉 ❤️ 🌟 🔥 ⚡ 🎯 ││
│  │ 📚 🎨 🎵 🎬 🏆 👑 💎 🔮 🌈 🦋 ││
│  └─────────────────────────────────────────┘│
│                                             │
│  Obsidian Icons:                            │
│  ┌─────────────────────────────────────────┐│
│  │ [lucide icons from obsidian]            ││
│  └─────────────────────────────────────────┘│
│                                             │
│  Icon Plugin (если установлен):             │
│  ┌─────────────────────────────────────────┐│
│  │ [icons from icon plugin]                ││
│  └─────────────────────────────────────────┘│
│                                             │
└─────────────────────────────────────────────┘
```

---

## 📱 Мобильная адаптивность

### Принципы адаптации

> ⚠️ **Важно**: Все размеры указаны в `rem`, не в `px`!

1. **Увеличенные области касания** — минимум `2.75rem × 2.75rem` (44×44 пикселя при базовом размере 16px)
2. **Fullscreen модалы** — на мобильных модалы занимают весь экран
3. **Свайп-жесты** — свайп для удаления/редактирования списков
4. **Sticky header** — фиксированный заголовок при скролле

### CSS для мобильных компонентов

```css
/* Адаптивные стили для Agenda — использует rem и CSS переменные Obsidian */

/* Базовые отступы и размеры */
.agenda-list {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;  /* 8px */
  padding: var(--size-4-2);
}

.agenda-item {
  min-height: 2.75rem;  /* 44px — минимальный touch target */
  padding: 0.75rem 1rem;  /* 12px 16px */
  background-color: var(--background-primary-alt);
  border-radius: var(--radius-m);
  border: 0.0625rem solid var(--background-modifier-border);
}

/* Мобильные модалы — fullscreen */
@media (max-width: 48rem) {  /* 768px */
  .agenda-settings-modal {
    position: fixed;
    inset: 0;  /* top/right/bottom/left: 0 */
    border-radius: 0;
    max-height: 100vh;
    max-width: 100vw;
  }
  
  .agenda-settings-header {
    position: sticky;
    top: 0;
    padding: 1rem;  /* 16px */
    background-color: var(--background-primary);
    border-bottom: 0.0625rem solid var(--background-modifier-border);
  }
  
  /* Увеличенные кнопки для touch */
  .agenda-action-btn {
    min-width: 2.75rem;  /* 44px */
    min-height: 2.75rem;  /* 44px */
    padding: 0.75rem;  /* 12px */
  }
}

/* Тёмная тема — автоматически через CSS переменные */
.theme-dark .agenda-item {
  /* Переменные Obsidian автоматически применяются */
  /* Не нужно переопределять цвета вручную */
}
```

### Mobile-first компоненты

```svelte
<!-- AgendaSettings.svelte -->
<script lang="ts">
  import { isMobile } from 'src/lib/stores/ui';
  
  // Все типы должны быть явными (TypeScript strict mode)
  export let onClose: () => void;
  export let onSave: (config: AgendaConfig) => void;
</script>

{#if $isMobile}
  <div class="agenda-settings-mobile fullscreen">
    <!-- Мобильная версия с увеличенными touch targets -->
    <header class="mobile-header">
      <button 
        class="close-btn" 
        on:click={onClose}
        style="min-width: 2.75rem; min-height: 2.75rem;"
      >
        ×
      </button>
      <h2>Настройка Агенды</h2>
      <button 
        class="save-btn" 
        on:click={() => onSave(config)}
        style="min-width: 2.75rem; min-height: 2.75rem;"
      >
        ✓
      </button>
    </header>
    <!-- ... -->
  </div>
{:else}
  <Modal>
    <!-- Desktop версия -->
  </Modal>
{/if}

<style>
  /* Все размеры в rem, цвета через CSS переменные */
  .mobile-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem;
    background-color: var(--background-primary);
    border-bottom: 0.0625rem solid var(--background-modifier-border);
  }
  
  .close-btn, .save-btn {
    min-width: 2.75rem;
    min-height: 2.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--interactive-normal);
    color: var(--text-normal);
    border: none;
    border-radius: var(--radius-s);
    cursor: pointer;
  }
  
  .close-btn:hover, .save-btn:hover {
    background-color: var(--interactive-hover);
  }
</style>
```

---

## 🗄️ Хранение данных

### Структура в settings.json

```json
{
  "projects": [{
    "id": "project-1",
    "name": "My Project",
    "agenda": {
      "mode": "custom",
      "standard": {
        "inheritCalendarFilters": true
      },
      "custom": {
        "lists": [
          {
            "id": "list-1",
            "name": "Срочные задачи",
            "icon": { "type": "emoji", "value": "🔥" },
            "order": 0,
            "filters": [
              {
                "id": "f1",
                "field": "priority",
                "operator": "equals",
                "value": "high",
                "conjunction": "AND"
              },
              {
                "id": "f2",
                "field": "due",
                "operator": "less_or_equal",
                "value": "today",
                "conjunction": "AND"
              }
            ]
          },
          {
            "id": "list-2",
            "name": "В работе",
            "icon": { "type": "lucide", "value": "clock" },
            "order": 1,
            "filters": [
              {
                "id": "f3",
                "field": "status",
                "operator": "equals",
                "value": "in-progress",
                "conjunction": "AND"
              }
            ]
          }
        ]
      }
    }
  }]
}
```

---

## 📋 План реализации

### Фаза 1: Базовая инфраструктура (v3.1.0)
- [ ] Добавить `AgendaConfig` в `ProjectDefinition`
- [ ] Создать переключатель режимов агенды в настройках проекта
- [ ] Реализовать `AgendaStandard.svelte` с наследованием фильтров

### Фаза 2: Конструктор списков (v3.2.0)
- [ ] Создать `AgendaCustom.svelte`
- [ ] Реализовать `AgendaListBuilder.svelte`
- [ ] Реализовать `AgendaFilterEditor.svelte`
- [ ] Добавить drag-and-drop для сортировки списков

### Фаза 3: Выбор иконок (v3.2.0)
- [ ] Создать `AgendaIconPicker.svelte`
- [ ] Интеграция с Lucide icons
- [ ] Поддержка emoji
- [ ] Интеграция с Icon Plugin (опционально)

### Фаза 4: Мобильная адаптация (v3.2.x)
- [ ] Fullscreen модалы для мобильных
- [ ] Увеличенные touch targets (`2.75rem` минимум)
- [ ] Свайп-жесты для редактирования
- [ ] Тестирование на iOS и Android

### Фаза 5: Улучшения (v3.3.0)
- [ ] Импорт/экспорт конфигурации агенды
- [ ] Шаблоны списков (presets)
- [ ] Цветовая кодировка списков

---

## 🔗 Связанные задачи

- [ ] #agenda-filters — Синхронизация фильтров календаря с агендой
- [ ] #agenda-custom — Конструктор кастомной агенды
- [ ] #agenda-icons — Система выбора иконок
- [ ] #agenda-mobile — Мобильная адаптация модалов

---

## ✅ Чеклист соответствия стандартам

Перед merge каждого PR по Agenda 2.0:

### Обязательные проверки

- [ ] **Единицы измерения**: Все размеры в `rem`, не в `px`
- [ ] **CSS переменные**: Используются токены Obsidian (`--background-primary`, `--text-normal`, etc.)
- [ ] **TypeScript strict**: Нет ошибок компиляции, нет необоснованных `any`
- [ ] **Promise handling**: Все промисы обработаны (`void`, `.catch()`, `await`)
- [ ] **localStorage**: Только через `App.loadLocalStorage()` / `App.saveLocalStorage()`
- [ ] **console.log**: Заменены на `console.debug()` или удалены
- [ ] **Тесты**: Добавлены unit-тесты для новой функциональности
- [ ] **Build**: `npm run build` — успешно
- [ ] **Lint**: `npm run lint` — 0 ошибок
- [ ] **Tests**: `npm test` — все тесты проходят

### Мобильная адаптивность

- [ ] Touch targets: минимум `2.75rem × 2.75rem` для кнопок и интерактивных элементов
- [ ] Media queries: используют `rem` (например, `@media (max-width: 48rem)`)
- [ ] Модалы: fullscreen на мобильных устройствах
- [ ] Тестирование: проверено на iOS Safari и Android Chrome

### Accessibility (a11y)

- [ ] Focus visible: все интерактивные элементы имеют видимый focus
- [ ] ARIA labels: кнопки без текста имеют `aria-label`
- [ ] Keyboard navigation: все действия доступны с клавиатуры
- [ ] Reduced motion: анимации отключаются при `prefers-reduced-motion`

---

## 📚 Ссылки

### Внутренняя документация
- [CODE_STANDARDS.md](CODE_STANDARDS.md) — Стандарты кода (EN)
- [CODE_STANDARDS-RU.md](CODE_STANDARDS-RU.md) — Стандарты кода (RU)
- [architecture-database-view.md](architecture-database-view.md) — Архитектура Database View
- [user-guide.md](user-guide.md) — Руководство пользователя

### Скрипты разработки
- [convert-px-to-rem.ps1](../scripts/convert-px-to-rem.ps1) — Конвертация px → rem

### Внешние ресурсы
- [Obsidian Plugin Guidelines](https://docs.obsidian.md/Plugins/Releasing/Plugin+guidelines)
- [Obsidian CSS Variables](https://docs.obsidian.md/Reference/CSS+variables)
- [Lucide Icons](https://lucide.dev/icons/)
- [Svelte Documentation](https://svelte.dev/docs)
