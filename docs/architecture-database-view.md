# 📊 Архитектура Database View

> **Статус:** Внутренний документ / Концептуальное проектирование  
> **Версия:** v3.3.0 (планируется)  
> **Последнее обновление:** 2026-01-27  
> **Тип:** Для рассуждений и обсуждения

---

## 📋 Содержание

- [Философия](#-философия)
- [Концепция](#-концепция)
- [Архитектурное видение](#-архитектурное-видение)
- [Модель данных](#-модель-данных)
- [Система формул](#-система-формул)
- [Визуализации](#-визуализации)
- [Конструктор дашбордов](#-конструктор-дашбордов)
- [Примеры использования](#-примеры-использования)
- [Технические решения](#-технические-решения)

---

## 💭 Философия

### Принципы проектирования

1. **Бесследность (Non-invasive)**
   - Плагин работает ТОЛЬКО с frontmatter полями
   - Никаких скрытых файлов или баз данных
   - Все данные остаются читаемыми и портативными
   - Markdown-файлы сохраняют свою структуру

2. **Данные как первоисточник**
   - Заметки = записи базы данных
   - Frontmatter = поля записи
   - Папки/теги = коллекции
   - Вычисления = динамические представления

3. **Конструктор, а не код**
   - Пользователь строит запросы визуально
   - Формулы похожи на таблицы (интуитивный синтаксис)
   - Нет необходимости в программировании

4. **Гибкость представления**
   - Одни данные → множество представлений
   - Таблицы, графики, галереи, списки
   - Настраиваемые дашборды

---

## 🎯 Концепция

### Что такое Database View?

Database View — это мощный конструктор для работы с данными заметок, который позволяет:

- **Агрегировать** — собирать данные из множества заметок
- **Вычислять** — применять формулы и функции
- **Визуализировать** — представлять данные в различных форматах
- **Трансформировать** — создавать производные данные

### Ключевая метафора

```
┌─────────────────────────────────────────────────────────────┐
│                      ВАШЕ ХРАНИЛИЩЕ                        │
│                                                             │
│   📝 Заметка 1    📝 Заметка 2    📝 Заметка 3    ...      │
│   ├─ title        ├─ title        ├─ title                 │
│   ├─ date         ├─ date         ├─ date                  │
│   ├─ amount       ├─ amount       ├─ amount                │
│   └─ category     └─ category     └─ category              │
│                                                             │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE VIEW                            │
│                                                             │
│   ┌─────────────────────────────────────────────────────┐  │
│   │ ЗАПРОС: Выбрать заметки где category = "расход"     │  │
│   │ ГРУППИРОВКА: По месяцам                             │  │
│   │ ВЫЧИСЛЕНИЕ: SUM(amount)                             │  │
│   └─────────────────────────────────────────────────────┘  │
│                                                             │
│                           │                                 │
│                           ▼                                 │
│                                                             │
│   ┌───────────┐  ┌───────────┐  ┌───────────┐              │
│   │  ТАБЛИЦА  │  │  ГРАФИК   │  │  SUMMARY  │              │
│   │           │  │     📈    │  │           │              │
│   │ Янв: 5000 │  │    ╱╲     │  │ Итого:    │              │
│   │ Фев: 3000 │  │   ╱  ╲    │  │ 15,000    │              │
│   │ Мар: 7000 │  │  ╱    ╲   │  │           │              │
│   └───────────┘  └───────────┘  └───────────┘              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Архитектурное видение

### Слои системы

```
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                       │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │
│  │ Table   │ │ Chart   │ │ Gallery │ │ Board   │          │
│  │ Render  │ │ Render  │ │ Render  │ │ Render  │          │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘          │
│       └──────────┬┴──────────┬┴──────────┬┘               │
│                  ▼           ▼           ▼                 │
│            ┌─────────────────────────────────┐             │
│            │      Dashboard Composer        │             │
│            └─────────────────────────────────┘             │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                    COMPUTATION LAYER                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 Formula Engine                       │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐         │   │
│  │  │ Aggregates│ │ Transforms│ │ Conditions│         │   │
│  │  │ SUM, AVG  │ │ DATE, NUM │ │ IF, AND   │         │   │
│  │  └───────────┘ └───────────┘ └───────────┘         │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 Query Builder                        │   │
│  │  SELECT → WHERE → GROUP BY → ORDER BY → LIMIT       │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────┬───────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────┐
│                      DATA LAYER                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              DataFrame (существующий)                │   │
│  │  ┌───────────┐ ┌───────────┐ ┌───────────┐         │   │
│  │  │ Records   │ │ Fields    │ │ Types     │         │   │
│  │  └───────────┘ └───────────┘ └───────────┘         │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           DataSources (существующий)                 │   │
│  │  folder │ tag │ frontmatter │ dataview              │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### Структура модулей

```
src/
├── lib/
│   ├── dataframe/              # ✅ Существует
│   ├── datasources/            # ✅ Существует
│   └── database/               # 🆕 Новый модуль
│       ├── index.ts
│       ├── QueryBuilder.ts           # Конструктор запросов
│       ├── FormulaEngine.ts          # Движок формул
│       ├── FormulaParser.ts          # Парсер формул
│       ├── FormulaFunctions.ts       # Библиотека функций
│       ├── Aggregator.ts             # Агрегация данных
│       ├── SummaryGenerator.ts       # Генерация саммари
│       └── types.ts
├── ui/
│   └── views/
│       └── Database/           # 🆕 Новое представление
│           ├── Database.svelte
│           ├── components/
│           │   ├── QueryEditor/
│           │   │   ├── QueryEditor.svelte
│           │   │   ├── FieldSelector.svelte
│           │   │   ├── FilterBuilder.svelte
│           │   │   └── GroupBySelector.svelte
│           │   ├── FormulaBar/
│           │   │   ├── FormulaBar.svelte
│           │   │   ├── FormulaInput.svelte
│           │   │   └── FunctionPicker.svelte
│           │   ├── Visualizations/
│           │   │   ├── DataTable.svelte
│           │   │   ├── Chart.svelte
│           │   │   ├── Gallery.svelte
│           │   │   ├── Board.svelte
│           │   │   └── Summary.svelte
│           │   └── Dashboard/
│           │       ├── DashboardCanvas.svelte
│           │       ├── Widget.svelte
│           │       ├── WidgetPalette.svelte
│           │       └── LayoutManager.ts
│           └── stores/
│               ├── queryStore.ts
│               └── formulaStore.ts
```

---

## 📦 Модель данных

### Расширение DataFrame

```typescript
// Существующий интерфейс (сохраняем)
interface DataFrame {
  records: DataRecord[];
  fields: DataField[];
}

// Расширение для Database View
interface DatabaseFrame extends DataFrame {
  // Метаданные запроса
  query: DatabaseQuery;
  
  // Вычисляемые поля
  computedFields: ComputedField[];
  
  // Агрегации
  aggregations: Aggregation[];
  
  // Группировки
  groups: DataGroup[];
}

interface ComputedField {
  name: string;
  formula: string;
  resultType: FieldType;
  dependencies: string[];  // Зависимые поля
}

interface Aggregation {
  field: string;
  function: AggregateFunction;
  alias: string;
  groupBy?: string[];
}

type AggregateFunction = 
  | 'SUM' | 'AVG' | 'MIN' | 'MAX' | 'COUNT'
  | 'COUNTIF' | 'SUMIF' | 'AVGIF'
  | 'FIRST' | 'LAST' | 'LIST' | 'UNIQUE';

interface DataGroup {
  key: Record<string, any>;  // Значения группировки
  records: DataRecord[];
  aggregates: Record<string, any>;
}
```

### DatabaseQuery

```typescript
interface DatabaseQuery {
  // Источник данных
  source: {
    type: 'folder' | 'tag' | 'frontmatter' | 'dataview';
    config: Record<string, any>;
  };
  
  // Выбор полей
  select: SelectClause[];
  
  // Фильтрация
  where: FilterCondition[];
  
  // Группировка
  groupBy: string[];
  
  // Агрегация (HAVING)
  having: FilterCondition[];
  
  // Сортировка
  orderBy: OrderClause[];
  
  // Лимит
  limit?: number;
  offset?: number;
}

interface SelectClause {
  field: string;           // Имя поля или '*'
  alias?: string;          // Псевдоним
  formula?: string;        // Вычисляемое поле
}

interface FilterCondition {
  field: string;
  operator: FilterOperator;
  value: any;
  logic?: 'AND' | 'OR';
}

type FilterOperator = 
  | 'eq' | 'neq' | 'gt' | 'gte' | 'lt' | 'lte'
  | 'contains' | 'startsWith' | 'endsWith'
  | 'in' | 'notIn'
  | 'isNull' | 'isNotNull'
  | 'between';
```

---

## 🔢 Система формул

### Синтаксис формул

Синтаксис интуитивно понятен и напоминает формулы электронных таблиц:

```
// Базовые операции
= A + B
= A * 1.2
= A / B * 100

// Функции
= SUM(amount)
= AVG(price)
= IF(status = "done", 1, 0)
= CONCAT(firstName, " ", lastName)

// Ссылки на поля
= @field_name
= @{field with spaces}

// Условия
= IF(amount > 1000, "high", IF(amount > 500, "medium", "low"))

// Работа с датами
= DAYS(@due_date, TODAY())
= MONTH(@date)
= YEAR(@date)

// Агрегация в контексте группы
= SUM(amount) / COUNT(*)
= AVG(price) * COUNT(*)
```

### Библиотека функций

```typescript
interface FormulaFunction {
  name: string;
  category: FunctionCategory;
  signature: string;
  description: string;
  examples: string[];
  evaluate: (args: any[], context: FormulaContext) => any;
}

type FunctionCategory = 
  | 'math'        // Математические
  | 'text'        // Текстовые
  | 'date'        // Даты
  | 'logical'     // Логические
  | 'aggregate'   // Агрегация
  | 'lookup';     // Поиск

// Пример: Функции по категориям
const FORMULA_FUNCTIONS: Record<FunctionCategory, FormulaFunction[]> = {
  math: [
    { name: 'SUM', signature: 'SUM(field)', ... },
    { name: 'AVG', signature: 'AVG(field)', ... },
    { name: 'MIN', signature: 'MIN(field)', ... },
    { name: 'MAX', signature: 'MAX(field)', ... },
    { name: 'COUNT', signature: 'COUNT(*) or COUNT(field)', ... },
    { name: 'ROUND', signature: 'ROUND(number, decimals)', ... },
    { name: 'ABS', signature: 'ABS(number)', ... },
    { name: 'POWER', signature: 'POWER(base, exponent)', ... },
  ],
  text: [
    { name: 'CONCAT', signature: 'CONCAT(text1, text2, ...)', ... },
    { name: 'UPPER', signature: 'UPPER(text)', ... },
    { name: 'LOWER', signature: 'LOWER(text)', ... },
    { name: 'TRIM', signature: 'TRIM(text)', ... },
    { name: 'LEFT', signature: 'LEFT(text, count)', ... },
    { name: 'RIGHT', signature: 'RIGHT(text, count)', ... },
    { name: 'LEN', signature: 'LEN(text)', ... },
    { name: 'REPLACE', signature: 'REPLACE(text, old, new)', ... },
  ],
  date: [
    { name: 'TODAY', signature: 'TODAY()', ... },
    { name: 'NOW', signature: 'NOW()', ... },
    { name: 'YEAR', signature: 'YEAR(date)', ... },
    { name: 'MONTH', signature: 'MONTH(date)', ... },
    { name: 'DAY', signature: 'DAY(date)', ... },
    { name: 'WEEKDAY', signature: 'WEEKDAY(date)', ... },
    { name: 'DAYS', signature: 'DAYS(end_date, start_date)', ... },
    { name: 'DATEADD', signature: 'DATEADD(date, number, unit)', ... },
  ],
  logical: [
    { name: 'IF', signature: 'IF(condition, true_value, false_value)', ... },
    { name: 'AND', signature: 'AND(condition1, condition2, ...)', ... },
    { name: 'OR', signature: 'OR(condition1, condition2, ...)', ... },
    { name: 'NOT', signature: 'NOT(condition)', ... },
    { name: 'SWITCH', signature: 'SWITCH(value, case1, result1, ...)', ... },
    { name: 'ISBLANK', signature: 'ISBLANK(value)', ... },
  ],
  aggregate: [
    { name: 'SUMIF', signature: 'SUMIF(field, condition)', ... },
    { name: 'COUNTIF', signature: 'COUNTIF(field, condition)', ... },
    { name: 'AVGIF', signature: 'AVGIF(field, condition)', ... },
    { name: 'LIST', signature: 'LIST(field)', ... },
    { name: 'UNIQUE', signature: 'UNIQUE(field)', ... },
    { name: 'FIRST', signature: 'FIRST(field)', ... },
    { name: 'LAST', signature: 'LAST(field)', ... },
  ],
  lookup: [
    { name: 'LOOKUP', signature: 'LOOKUP(value, source, result_field)', ... },
    { name: 'RELATED', signature: 'RELATED(linked_field)', ... },
  ],
};
```

### FormulaEngine

```typescript
class FormulaEngine {
  private parser: FormulaParser;
  private functions: Map<string, FormulaFunction>;
  
  constructor() {
    this.parser = new FormulaParser();
    this.functions = new Map();
    this.registerBuiltinFunctions();
  }
  
  // Парсинг и валидация формулы
  parse(formula: string): ParsedFormula {
    return this.parser.parse(formula);
  }
  
  // Вычисление для одной записи
  evaluate(formula: string, record: DataRecord): any {
    const parsed = this.parse(formula);
    return this.executeAST(parsed.ast, { record });
  }
  
  // Вычисление для группы (агрегация)
  evaluateAggregate(formula: string, records: DataRecord[]): any {
    const parsed = this.parse(formula);
    return this.executeAST(parsed.ast, { records, isAggregate: true });
  }
  
  // Получение зависимостей формулы
  getDependencies(formula: string): string[] {
    const parsed = this.parse(formula);
    return this.extractFieldReferences(parsed.ast);
  }
  
  // Регистрация пользовательской функции
  registerFunction(fn: FormulaFunction): void {
    this.functions.set(fn.name.toUpperCase(), fn);
  }
}
```

---

## 📈 Визуализации

### Типы визуализаций

```typescript
type VisualizationType = 
  | 'table'       // Таблица с сортировкой и фильтрацией
  | 'chart'       // Графики (line, bar, pie, area)
  | 'gallery'     // Галерея карточек
  | 'board'       // Канбан-доска
  | 'calendar'    // Календарь (интеграция с существующим)
  | 'list'        // Вложенные списки
  | 'summary'     // Карточки с метриками
  | 'timeline';   // Временная шкала

interface VisualizationConfig {
  type: VisualizationType;
  title?: string;
  
  // Маппинг данных на визуализацию
  mapping: DataMapping;
  
  // Специфичные настройки
  options: Record<string, any>;
  
  // Стилизация
  style?: VisualizationStyle;
}

interface DataMapping {
  // Для таблиц
  columns?: ColumnConfig[];
  
  // Для графиков
  xAxis?: string;
  yAxis?: string | string[];
  series?: string;
  
  // Для галерей/досок
  cardTitle?: string;
  cardImage?: string;
  cardDescription?: string;
  groupField?: string;
}
```

### Chart Configuration

```typescript
interface ChartConfig extends VisualizationConfig {
  type: 'chart';
  options: {
    chartType: 'line' | 'bar' | 'pie' | 'area' | 'scatter';
    stacked?: boolean;
    showLegend?: boolean;
    showGrid?: boolean;
    colors?: string[];
    
    // Оси
    xAxis?: {
      label?: string;
      format?: string;  // Форматирование
    };
    yAxis?: {
      label?: string;
      min?: number;
      max?: number;
    };
  };
}
```

### Summary Cards

```typescript
interface SummaryConfig extends VisualizationConfig {
  type: 'summary';
  options: {
    metrics: MetricConfig[];
    layout: 'horizontal' | 'vertical' | 'grid';
  };
}

interface MetricConfig {
  label: string;
  formula: string;           // Формула для вычисления
  format?: 'number' | 'currency' | 'percent' | 'date';
  prefix?: string;
  suffix?: string;
  color?: string;
  icon?: string;
  
  // Сравнение
  comparison?: {
    formula: string;         // Формула для сравнения
    mode: 'absolute' | 'percent';
    positiveIsGood: boolean;
  };
}
```

---

## 🎨 Конструктор дашбордов

### Dashboard Architecture

```typescript
interface Dashboard {
  id: string;
  name: string;
  description?: string;
  
  // Источник данных
  query: DatabaseQuery;
  
  // Компоновка
  layout: DashboardLayout;
  
  // Виджеты
  widgets: Widget[];
  
  // Глобальные фильтры
  filters: DashboardFilter[];
  
  // Настройки обновления
  refresh?: {
    auto: boolean;
    interval: number;  // В секундах
  };
}

interface DashboardLayout {
  type: 'grid' | 'free';
  columns?: number;
  rowHeight?: number;
  gap?: number;
}

interface Widget {
  id: string;
  type: VisualizationType;
  title: string;
  
  // Позиция и размер
  position: {
    x: number;
    y: number;
    width: number;
    height: number;
  };
  
  // Конфигурация визуализации
  visualization: VisualizationConfig;
  
  // Дополнительный фильтр (сужение данных)
  filter?: FilterCondition[];
}

interface DashboardFilter {
  id: string;
  field: string;
  label: string;
  type: 'select' | 'multiselect' | 'date-range' | 'search';
  defaultValue?: any;
}
```

### Dashboard Composer (UI)

```svelte
<!-- DashboardCanvas.svelte -->
<script lang="ts">
  import { onMount } from 'svelte';
  import Widget from './Widget.svelte';
  import WidgetPalette from './WidgetPalette.svelte';
  import FilterBar from './FilterBar.svelte';
  
  export let dashboard: Dashboard;
  export let data: DatabaseFrame;
  
  let isEditing = false;
  let selectedWidget: Widget | null = null;
  
  function addWidget(type: VisualizationType) {
    // Добавление нового виджета
  }
  
  function updateWidgetPosition(id: string, position: Position) {
    // Обновление позиции виджета (drag & drop)
  }
  
  function updateWidgetSize(id: string, size: Size) {
    // Изменение размера виджета
  }
</script>

<div class="dashboard">
  <header class="dashboard-header">
    <h1>{dashboard.name}</h1>
    <FilterBar filters={dashboard.filters} on:change={handleFilterChange} />
    <button on:click={() => isEditing = !isEditing}>
      {isEditing ? 'Готово' : 'Редактировать'}
    </button>
  </header>
  
  <div class="dashboard-canvas" class:editing={isEditing}>
    {#each dashboard.widgets as widget (widget.id)}
      <Widget 
        {widget}
        {data}
        editable={isEditing}
        selected={selectedWidget?.id === widget.id}
        on:select={() => selectedWidget = widget}
        on:move={e => updateWidgetPosition(widget.id, e.detail)}
        on:resize={e => updateWidgetSize(widget.id, e.detail)}
      />
    {/each}
  </div>
  
  {#if isEditing}
    <WidgetPalette on:add={e => addWidget(e.detail.type)} />
  {/if}
</div>
```

---

## 📋 Примеры использования

### Пример 1: Финансовый трекер

**Структура заметок (frontmatter):**
```yaml
---
type: transaction
date: 2026-01-15
amount: 5000
category: salary
account: main
---
```

**Запрос в Database View:**
```
SELECT 
  MONTH(date) as month,
  category,
  SUM(amount) as total
FROM folder:"Finance"
WHERE YEAR(date) = 2026
GROUP BY MONTH(date), category
ORDER BY month ASC
```

**Дашборд:**
```
┌─────────────────────────────────────────────────────────────┐
│                    💰 Финансовый дашборд                    │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Доходы   │  │ Расходы  │  │ Баланс   │  │ Экономия │   │
│  │ 150,000  │  │ 87,000   │  │ 63,000   │  │ 42%      │   │
│  │ ↑ +12%   │  │ ↓ -5%    │  │ ↑ +35%   │  │ ↑ +8%    │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│                                                             │
│  ┌────────────────────────┐  ┌────────────────────────┐    │
│  │   📊 Расходы по        │  │   📈 Динамика          │    │
│  │      категориям        │  │      за год            │    │
│  │                        │  │                        │    │
│  │   🍕 Еда      25%     │  │   ╱──╲    ╱╲          │    │
│  │   🏠 Жильё    35%     │  │  ╱    ╲──╱  ╲──       │    │
│  │   🚗 Транспорт 15%    │  │ ╱                      │    │
│  │   🎮 Развлечения 25%  │  │                        │    │
│  └────────────────────────┘  └────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### Пример 2: Трекер проектов

**Структура заметок:**
```yaml
---
type: task
project: "Website Redesign"
status: in-progress
priority: high
estimate: 8
actual: 5
assignee: Pavel
due: 2026-02-01
---
```

**Формулы:**
```
// Прогресс проекта
= COUNT(status = "done") / COUNT(*) * 100

// Превышение оценки
= SUM(actual) / SUM(estimate) * 100

// Дней до дедлайна
= MIN(DAYS(due, TODAY()))
```

### Пример 3: Вывод в саммари-заметку

**Сценарий:** Автоматическое создание/обновление заметки-саммари с агрегированными данными.

```typescript
interface SummaryOutput {
  // Целевая заметка
  target: {
    path: string;           // Путь к файлу
    createIfNotExists: boolean;
  };
  
  // Поля для записи во frontmatter
  fields: SummaryField[];
  
  // Триггер обновления
  trigger: 'manual' | 'on-change' | 'scheduled';
}

interface SummaryField {
  name: string;             // Имя поля frontmatter
  formula: string;          // Формула для вычисления
  format?: string;          // Форматирование
}
```

**Пример конфигурации:**
```yaml
# Конфигурация вывода в саммари
output:
  target: "Reports/Monthly Summary.md"
  createIfNotExists: true
  fields:
    - name: total_income
      formula: SUM(amount) WHERE category = "income"
    - name: total_expense  
      formula: SUM(amount) WHERE category = "expense"
    - name: balance
      formula: "@total_income - @total_expense"
    - name: top_category
      formula: FIRST(category ORDER BY SUM(amount) DESC)
    - name: updated_at
      formula: NOW()
```

**Результат в заметке:**
```yaml
---
total_income: 150000
total_expense: 87000
balance: 63000
top_category: "salary"
updated_at: 2026-01-27T14:30:00
---

# Месячная сводка

> Автоматически сгенерировано Database View
```

---

## ⚙️ Технические решения

### Производительность

1. **Ленивые вычисления**
   - Формулы вычисляются только при необходимости
   - Кэширование промежуточных результатов
   - Инкрементальные обновления при изменении данных

2. **Web Workers**
   - Тяжёлые вычисления выносятся в отдельный поток
   - UI остаётся отзывчивым

3. **Виртуализация**
   - Для больших таблиц — виртуальный скроллинг
   - Рендеринг только видимых элементов

### Безопасность формул

```typescript
class FormulaSandbox {
  // Белый список разрешённых операций
  private allowedOperators = ['+', '-', '*', '/', '%', '==', '!=', '<', '>', '<=', '>='];
  
  // Запрет на:
  // - eval()
  // - Function()
  // - Доступ к window/document
  // - Бесконечные циклы (timeout)
  
  execute(ast: AST, context: Context, timeout: number = 5000): any {
    return new Promise((resolve, reject) => {
      const timer = setTimeout(() => {
        reject(new Error('Formula execution timeout'));
      }, timeout);
      
      try {
        const result = this.evaluate(ast, context);
        clearTimeout(timer);
        resolve(result);
      } catch (error) {
        clearTimeout(timer);
        reject(error);
      }
    });
  }
}
```

### Хранение конфигурации

Конфигурация Database View хранится в настройках проекта (принцип бесследности):

```typescript
interface ProjectSettings {
  // Существующие настройки...
  
  // Конфигурация Database View
  databaseViews?: DatabaseViewConfig[];
}

interface DatabaseViewConfig {
  id: string;
  name: string;
  query: DatabaseQuery;
  dashboard?: Dashboard;
  
  // Не создаёт отдельных файлов!
  // Всё хранится в data.json проекта
}
```

---

## 🔗 Связи с существующей архитектурой

### Интеграция с DataFrame

```
Существующий DataFrame
        │
        ▼
┌───────────────────┐
│ DatabaseFrame     │ ← Расширение, не замена!
│ extends DataFrame │
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ FormulaEngine     │ ← Новый слой поверх данных
└───────────────────┘
        │
        ▼
┌───────────────────┐
│ Visualizations    │ ← Новые компоненты представления
└───────────────────┘
```

### Совместимость с представлениями

Database View может использовать существующие компоненты:
- Calendar view для временных данных
- Board view для канбан-представления
- Table view как базовый рендер

---

## 📅 План реализации

### Фаза 1: Foundation (v3.3.0-alpha)

- [ ] FormulaParser — парсер формул
- [ ] FormulaEngine — базовые функции (math, text)
- [ ] QueryBuilder — визуальный конструктор запросов
- [ ] DataTable — улучшенный табличный вид

### Фаза 2: Core Features (v3.3.0-beta)

- [ ] Все категории функций
- [ ] Группировка и агрегация
- [ ] Chart компонент (line, bar, pie)
- [ ] Summary cards

### Фаза 3: Dashboard (v3.3.0)

- [ ] DashboardCanvas — сетка виджетов
- [ ] Drag & drop виджетов
- [ ] Глобальные фильтры
- [ ] Экспорт в саммари-заметки

### Фаза 4: Advanced (v3.4.0+)

- [ ] Lookup между проектами
- [ ] Пользовательские функции
- [ ] Шаблоны дашбордов
- [ ] Автоматизация (scheduled updates)

---

## 📝 Примечания для разработки

> **Важно:** Этот документ является концептуальным и служит основой для обсуждения. Конкретная реализация может отличаться.

### Открытые вопросы

1. **Синтаксис формул** — использовать синтаксис таблиц или свой DSL?
2. **Хранение дашбордов** — в проекте или отдельные файлы?
3. **Интеграция с Dataview** — использовать DQL или свой синтаксис?
4. **Графики** — библиотека Chart.js или собственные SVG?

### Риски

- Сложность парсера формул
- Производительность на больших данных
- UX конструктора — баланс мощности и простоты

---

> **Последнее обновление:** 2026-01-27  
> **Автор:** Projects Plus Team
