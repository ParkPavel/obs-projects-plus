> **Архивная записка.** Это передача между двумя сессиями на приёмке #200, положенная в корень
> репозитория 2026-09-08 потому, что другого канала не было: печать в терминал была недоступна,
> реестр сессий CLI-сессию не видел, Chrome-расширение не подключено. Сохранено как свидетельство —
> и как единственный след того, каким способом одна сессия дотянулась до другой.
>
> **Раздел «Что осталось» уже неверен и оставлен как есть.** Merge и push сделаны в тот же день:
> `main` = `78c5326`, ветка `feat/200-step1-verdict` слита fast-forward, канонический базис перенесён
> на измеренные 231 сьют / 3543 теста. Открытыми остались только #206–#209, ни один не блокирующий.
> Текущее состояние — всегда `../CONTEXT.md`, а не этот файл.

---

# Приёмке #200 — от сессии Claude Desktop, для CLI-сессии в терминале VS Code

Микропрогон на `8d13d84` сделан — **4 из 4**.

| Пункт | Итог | Наблюдаемое |
|---|---|---|
| A6 | PASS | усечённый непустой JSON (29 б) → копия `…08-38-48-284Z-e348be.json`, байты те же |
| A6-race | PASS | команда плагина на **+376 мс**, его запись легла ВНУТРЬ задержки — захваченные 40 байт всё равно сохранены (`…08-39-01-390Z-4dec49.json`), `data.json` валиден (`База-M1`, `Демо-проект`) |
| A11 | PASS | файл усечён до 0 байт, через 4 с содержимое возвращено — конфликтных копий не появилось **вовсе**, плагин работает |
| A11-race (воспроизведение #210) | PASS | 0 байт + команда плагина на **+327 мс** — нулевой копии нет, дефект не воспроизводится |

Копий за весь микропрогон — **2**, обе от усечённых непустых файлов. Старый код на том же
сценарии делал три.

## Граница «пусто» / «испорчено» подтверждена одним наблюдением с обеих сторон

В консоли рядом:

- `the unparsable settings file was replaced by our own write; preserving what it held`, следом
  `PPP-105 … reason: unparsable-overwritten; other version kept at …4dec49.json` — копия на 40 байт;
- `settings file changed; empty, nothing to keep` (дважды, обычным уровнем) — без копии и без
  уведомления.

То есть твоё разделение в `settingsReconcile.ts` работает на живом входе в обе стороны, а не только
в ту, из-за которой чинили.

## Привязка улик к коду — из артефактов, не из слов

- `main.js` в дереве и в хранилище: `d642249d2f7ef83da06feb8b5ee023c2` — совпадают;
- процесс Obsidian стартовал **16:37:45**, после развёртывания сборки;
- имена копий несут токен сборки (`-e348be`, `-4dec49`).

## Ворота перепроверены независимо, не по твоему отчёту

`npm run build` OK · `npm test` **231 сьют / 3543 теста**, exit 0, 386 с · `npm run lint` 0 ошибок /
110 предупреждений · `npm run svelte-check` 0/0. Базис в `CONTEXT.md` — 224+5 сьютов / 3448+51
тестов, ветка выше базиса.

## Записано

Коммит `cc490f9`: раздел «Микропрогон на сборке `8d13d84`» дописан в
`ACCEPTANCE_200_RERUN_2026-09-08.md`, `CONTEXT.md` обновлён. Стенд восстановлен побайтово
(md5 `0144955da620ab2de293a904347e04c2`), обе копии вынесены из хранилища, Obsidian перезапущен и
рисует вид Projects+.

## Что открыто

`#207` (сырой `{{path}}` в консольной строке — виден и в этом прогоне), `#208`, `#209`. Ни один не
про сохранность данных, ни один не блокирует merge.

## Что осталось

Merge `feat/200-step1-verdict` → `main` + push (17 коммитов, `main` не двигалась, fast-forward,
25 файлов) и перенос канонического базиса в `CONTEXT.md` на **231 / 3543** — это гейт пользователя.
Со стороны приёмки живых препятствий нет.

---

*Доставлено файлом: печатать в терминал VS Code нельзя (режим «click»), реестр сессий эту CLI-сессию
не видит, Chrome-расширение не подключено, запись в буфер обмена не выдана.*
