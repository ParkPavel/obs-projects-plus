# Untestable Features Report — стек W2 (#095/#094/#104/#092/#075)

> Прогон: 2026-06-22, автономный API-тест по `MANUAL_TESTING_PIPELINE.md`.
> Ветка `feat/095-pipeline-value-placeholder` (7 коммитов, НЕ слита). Билд собран начисто
> (`npm run build` exit 0, main.js 14:51) и задеплоен в OBStests; `app:reload` выполнен,
> 11 команд плагина зарегистрированы. Baseline Jest 159/2264.

## Что API подтвердил автоматически (зелёное)

| Проверка | Результат |
|---|---|
| Deploy 3 артефактов (main.js/styles.css/manifest.json) | ✅ свежие |
| `app:reload` + регистрация команд | ✅ 11/11 |
| Smoke A1 — папка демо | ✅ `Projects Plus - Демо/` |
| Smoke A2 — записи | ✅ 28 `.md` |
| Smoke A3 — проект | ✅ `dataSource.kind=folder`, `version=4` |
| Smoke A4 — 5 вью | ✅ Обзор/Pipeline/График/Клиенты/Портфолио |
| Smoke A5 — композиция | ✅ (см. примечание) Обзор: stats+chart+4×database-call; Клиенты: stats+database-call |
| Smoke A6 — frontmatter | ✅ relation как wikilink (`client: [[Orbit Media]]`), даты `startDate` |
| Smoke A7 — активация вью | ✅ HTTP 204 |
| Roundtrip write/read/delete | ✅ PUT→GET(совпало)→DELETE(404) |
| **Данные-гейт для #094/#104** | ✅ `projects[0].fieldConfig.status.statusGroups` маппит ВСЕ 6 сырых ключей в 3 бакета — ни одно значение не падает в «No Status» |

**Примечание A5**: композиция отличается от baseline 2026-06-11 (`Клиенты` была `data-table`).
Это ожидаемая миграция data-table→database-call (#068/#074) + связанная пара «Клиенты→Проекты»
(linkedSelection=True на 4-м блоке Обзора). Не регрессия.

**Распределение статусов в демо** (сырые ключи, кормят бакет-логику):
`todo:4, doing:3, done:3, inProgress:3, planning:2, review:2`.
Маппинг `statusGroups`: todo=[planning,todo] · inProgress=[inProgress,doing,review] · complete=[done].

## Что REST API НЕ видит — нужен визуальный прогон человеком

> REST API не наблюдает рендер Svelte, легенды, CSS, hover/click, консоль.
> `POST /commands` возвращает 2xx даже если вью упало при рендере.

| # | Фича (тикет) | Шаги в OBStests | Ожидание |
|---|---|---|---|
| 1 | **#094 чарт-легенда** | Открыть демо → вью «Обзор» → виджет-чарт по статусу | Легенда показывает **«To Do / In Progress / Done»** (семантические бакеты), НЕ сырые `todo/doing/inProgress/planning/review/done`. Все записи распределены, бара «No Status» нет (данные это позволяют) |
| 2 | **#104 Board семантические колонки** | Вью «Pipeline» (board) | Колонки сгруппированы как **To Do / In Progress / Done / No Status** в этом порядке — идентично легенде чарта (один источник `bucketLabelForRaw`) |
| 3 | **#095 value-placeholder** | Любой database-call → ⋯ → Конвейер → добавить шаг «Фильтр» → поле значения | Плейсхолдер инпута = **«Значение…»** (локализованный `common.value-placeholder`), не «Value» |
| 4 | **#092 recovery-узел** | database-call → Конвейер → добавить шаг «Фильтр», условие, отсекающее ВСЕ записи → закрыть редактор | Виджет показывает EmptyState `filter-x` **«Конвейер скрыл все записи (N шагов)»** + кнопки **[Открыть конвейер]** / **[Очистить конвейер]**. N = число шагов |
| 5 | **#092 clear из recovery** | В состоянии (4) нажать **[Очистить конвейер]** | Один клик → шаги конвейера удалены, записи вернулись, виджет рендерит данные. Источник не тронут |
| 6 | **#092 НЕ ложное срабатывание** | Открыть «Обзор» (4-й блок с linkedSelection, без активного выбора → 0 записей по связи) | Recovery-узел НЕ показывается, когда пусто из-за linked source (а не из-за конвейера). Должен быть обычный empty / no-matches |
| 7 | **#075 clear-кнопка в редакторе** | Открыть PipelineEditor с ≥1 шагом | В футере есть кнопка **«Очистить конвейер»** (`trash-2`), один клик чистит все шаги |
| 8 | **#075 обучающий empty-state** | Открыть PipelineEditor БЕЗ шагов | Empty-state **«было→стало»** с иконками `wand-2`+`arrow-right` и примером (а не плоский текст) |
| 9 | **#075 копирайт 2A** | Навести на ∑-кнопку конвейера; открыть ⋯-меню виджета | Tooltip «на языке задач» (Конвейер: преобразуйте данные…); пункт меню «Конвейер преобразования данных». Во всех 4 локалях — без сырых ключей |
| 10 | **i18n локали** | Переключить язык Obsidian на uk и zh-CN, повторить #092/#075 | Все строки переведены, 0 сырых ключей `database-call.*` / `pipeline.*` |
| 11 | **Темы** | dark/light | Токены `--ppp-*` адаптируются, без хардкод-цветов в новых поверхностях |

## Вердикт

- **Все API-наблюдаемые гейты зелёные** — данные, контракт frontmatter, структура демо, загрузка
  плагина и (ключевое) данные-гейт `statusGroups` для семантических бакетов #094/#104 в порядке.
- **Логика покрыта Jest** (159 suites / 2264 tests), включая `bucketLabelForRaw`,
  `chartDataPipeline` (бакетизация) и `pipelineRecovery.test.ts`.
- **Осталось пиксельное** — 11 пунктов выше требуют глаз человека (рендер легенды, recovery-узел,
  empty-state, локали, темы). API доказал, что данные и движок готовы их отрисовать корректно.
