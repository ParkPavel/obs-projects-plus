# VISION COMPLIANCE — сверка построенного с мастер-промптом пользователя

> 2026-06-12. Сверено: 8 законов × 10 инвариантов × анти-паттерны мастер-промпта против
> кодовой базы @ HEAD и всех specs/. Нарушения МОЕГО кода исправлены в этом же коммите;
> унаследованные — затикечены. Документ обновляется при каждом большом аудите.

## Законы Vision

| Закон | Статус | Доказательства / разрывы |
|---|---|---|
| 1. Папка уже база | ✅ | cellDisplay деградирует мягко (неизвестное→текст, пустое→пусто); `[+]` создаёт поле с подтверждением, не требует схемы заранее |
| 2. Заметка = запись | ⚠ | Все записи через viewApi.updateRecord ✅; НО CalendarView держит локальные копии records (#078), EditNote — снапшот (#101). Оба затикечены, новые механизмы не добавляли промежуточных состояний |
| 3. Прозрачность контракта | ⚠→✅ | Tooltip заголовка = frontmatter-ключ (W1) ✅; double-click→файл — #060 открыт |
| 4. WikiLink — внешний ключ | ✅ (исправлено сейчас) | **НАРУШАЛ Я**: tableCanon нёс параллельный wikilink-парсер — удалён, всё через канонический `parseRelationLinks`; relations/enrichWithBacklinks используются ✅ |
| 5. Фильтр — это база | ⚠ | filterEvaluator един ✅ (ratchet); FilterPanel НЕ един: PipelineEditor несёт свой filter-UI — лечится #099 (один builder). `applySearch` таблицы — substring-поиск, НЕ фильтр-механизм (Notion: search-in-view отдельно) — зафиксировано как осознанное решение |
| 6. Подсказывай, а не требуй | ✅ | SmartSuggest, unnest-подсказка, wizard'ы, дефолты вкладок/виджетов |
| 7. Интуитивность | ⚠ | Большой прогресс (W1, R3-P0 меню, словарь конвейера); панели — W2 (#090/#093), модалка EditNote всё ещё «секционная простыня» |
| 8. Всё — файлы | ⚠ явно | data.json-компромисс зафиксирован НЕ молча — #066 ждёт решения пользователя |

## Инварианты мастер-промпта

| # | Инвариант | Статус |
|---|---|---|
| 1 | Canvas ≤200; виджет ≤300 (DBCall ≤400) | ✅ восстановлено сейчас: canvas 213→**198** (suggest/formula/escape вынесены в контроллеры), бюджет R0_6 возвращён на 200. DatabaseCallBlock 370 ≤400 ✅. **PipelineEditor 1278 — нарушение**, путь: #099 (расщепление сократит) |
| 2 | dashboard-engine чистый | ✅ проверено: 0 импортов obsidian/svelte |
| 3 | filterEvaluator един | ✅ |
| 4 | FilterPanel един | ⚠ PipelineEditor — #099 |
| 5 | Grid-only, нет FreeCanvas | ✅ |
| 6 | Selection Bus заморожен | ✅ — driver R3 добавлен через ПУБЛИЧНЫЙ API (setSelection/dataTableSourceId), интерфейс не менялся |
| 7 | Не трогать dataframe/formula/relations/виды вне Dashboard | ⚠ ОТСТУПЛЕНИЕ С САНКЦИЕЙ: #070 правил CalendarView/EditNote/FieldControl — по прямым дефектам пользовательского тестирования (его приоритет выше); dataframe/formula/relations не тронуты ✅ |
| 8 | Не rebuild архивных | ✅ обратное: containment R0_4 |
| 9 | Токены, px-budget ≤186 | ✅ 186 держится; hex-долг старого кода — #079 |
| 10 | 0 @ts-ignore; не в main | ✅ |

## Анти-паттерны: найдено у себя и исправлено/затикечено

1. **Параллельный wikilink-парсер** (tableCanon) — исправлен сейчас (→ parseRelationLinks).
2. **Тихое поднятие LOC-бюджета канваса** 200→230 — откатил: канвас ужат до 198, бюджет 200.
3. Дубли `baseName()` в 3 файлах (tableRowOps/TableRow/RelationPicker) — мелкий долг, в W5-чистку.
4. Протухший базлайн в SPEC §11 — заменён ссылкой на канон (CONTEXT.md), как в tester.md.

## Протокол конфликтов — открытые решения пользователя

#066 (данные-конфиг ↔ Закон 8), #080 (Formula Node), drag-to-link (#062, V3 зафиксирован).

## Вывод

Архитектурный слой соответствует мастер-промпту почти полностью; продуктовый слой движется
по волнам NOTION_GRADE_PIPELINE + NOTION_DM_RESEARCH (они — операционализация Законов 5–7).
Главные оставшиеся несоответствия сосредоточены в одном месте — старом PipelineEditor —
и устраняются эпиком #099.
