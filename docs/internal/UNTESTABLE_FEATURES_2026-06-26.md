# Untestable Features Report — фикс-стек 2026-06-26 (#105/#106/#107/#108/#109)

> Прогон: 2026-06-26, фикс-сессия по дефектам из `TEST_REPORT_2026-06-26.md`.
> Ветка `feat/095-pipeline-value-placeholder`, HEAD `f558b48` — все правки #105–#109
> **в рабочем дереве, НЕ закоммичены/слиты/запушены** (гейт пользователя).
> Билд пересобран начисто (`npm run build` exit 0) и задеплоен в OBStests
> (`main.js` + `styles.css` + `manifest.json`); `app:reload` выполнен, 11 команд плагина
> зарегистрированы.
>
> **4 гейта подтверждены независимым прогоном** (не со слов субагента):
> build 0 ошибок · jest **162 suites / 2287 tests PASS** · lint 0 errors · svelte-check 0/0.
> Baseline вырос со 159/2264 (старт волны) → 162/2287 за счёт новых regression-тестов
> (`pipelineSteps`/`PipelineEditor.mutation`, `relationFilterAdapter`, `canvasSelectionStore`
> escape, `chartDataPipeline` group-mode, `filterHelpers`, `boardHelpers`).

## Что API / гейты подтвердили автоматически (зелёное)

| Проверка | Результат |
|---|---|
| Deploy 3 артефактов (main.js/styles.css/manifest.json) | ✅ свежие |
| `app:reload` + регистрация команд | ✅ 11/11 `obs-projects-plus:*` |
| Smoke — папка/записи демо | ✅ `Projects Plus - Демо/`, 28 `.md` |
| Smoke — проект/вью | ✅ `dataSource.kind=folder` v4, 5 вью |
| Smoke — frontmatter | ✅ relation как wikilink (`client: [[Acme Studio]]`), 4-парам. даты |
| Roundtrip write/read/delete | ✅ PUT→GET(совпало)→DELETE(404) |
| Данные-гейт `statusGroups` (#107) | ✅ `fieldConfig.status.statusGroups` маппит 6 сырых ключей в 3 бакета (`todo:[planning,todo]`, `inProgress:[inProgress,doing,review]`, `complete:[done]`) |
| PX-ratchet (инвариант #6) | ✅ 177, без изменений (CSS не трогался в #105–#109) |
| `@ts-ignore` (инвариант #5) | ✅ 0 новых |

## Что REST API / Jest НЕ видят — нужен визуальный прогон человеком

> REST не наблюдает рендер Svelte, легенды, CSS, hover/click, консоль, переключение
> локали; `POST /commands` возвращает 2xx даже если вью упало при рендере. По выводу
> `TEST_REPORT_2026-06-26.md` — Jest здесь **недостаточен как единственный гейт**: юнит-тесты
> вызывают функции прямыми аргументами, минуя реальные дефолты компонентов и реальный рендер.
> Прогон выполнять в OBStests **после `Ctrl+R`** (reload), проект «Projects Plus - Демо».

| # | Тикет / дефект | Шаги в OBStests | Ожидание (PASS) |
|---|---|---|---|
| 1 | **#105** краш конвейера (Деф. 1, §4.3) | Обзор → любой database-call → ⋯ → «Конвейер данных» → на **существующем** шаге: клик 👁 «Отключить шаг» | Шаг отключается, **БЕЗ** `TypeError: Cannot assign to read only property` в консоли (Ctrl+Shift+I). Счётчик N→M пересчитывается |
| 2 | **#105** (там же) | На шаге «Группировка» сменить поле в селекте «Group by field» | Конфиг шага обновляется, заголовок шага меняется с «(не задано)», счётчик пересчитывается, без краша |
| 3 | **#105** (там же) | На шаге «Фильтр» нажать «+ Add condition» (на свежем И на существующем шаге) | Условие добавляется, без краша |
| 4 | **#106** Selection Bus Сценарий A (Деф. 2, §4.4) | Обзор → мастер-блок «Клиенты» → hover строки «Acme Studio» → ⋯ → «Фильтровать связанные блоки по этой строке» | Приёмник «Проекты клиента» показывает **2 проекта Acme Studio** (Redesign, Onboarding Flow), НЕ «Нет совпадений». Строка-мастер подсвечена (фиолетовая полоса) |
| 5 | **#106** Escape (Деф. 2, §4.4/§3.4) | В состоянии (4) нажать **Escape** | Выбор и подсветка снимаются по всему канвасу, приёмник возвращается к полному списку (8 записей) |
| 6 | **#106** регресс-контроль Сценарий B | Клик по сектору donut-чарта («inProgress») | По-прежнему **корректно** фильтрует stats-виджет, повторный клик снимает (не сломали при фиксе A) |
| 7 | **#107** чарт семантический toggle (Деф. 3, §4.2) | Обзор → edit dashboard → конфиг чарта «Проекты по статусу» | Появляется select **«Группировка статусов»** (только если у X-поля есть `statusGroups`). Дефолт = **Values** → легенда показывает сырые ключи `inProgress/done/planning/review` |
| 8 | **#107** чарт → Semantic | Переключить select в **Semantic groups** | Легенда donut сворачивается в **3 бакета: To Do / In Progress / Done**. Обратно в Values → снова сырые ключи (дефолт не изменился) |
| 9 | **#107** Board семантический toggle (#104) | Вью «Pipeline» (board) → настройки → «Группировка колонок» → **Semantic** | Колонки сворачиваются в бакеты **To Do / In Progress / Done** (+ No Status при наличии). Дефолт Values оставляет сырые колонки |
| 10 | **#108** i18n оператора (Деф. 4, §4.6) | Сменить язык Obsidian на **uk** (Українська, Перезапуск) → открыть pill активного фильтра вида | Оператор `is` отображается как **«Дорівнює»** (uk), НЕ русское «Равно». Повторить для **zh-CN** |
| 11 | **#109** дубль поля карточки (Деф. 5) | Вью «Pipeline» → настройки → Header field = `client` → карточка | Поле `client` показано **один раз** (как заголовок), из тела карточки убрано (нет дубля подряд) |
| 12 | (опц.) ранее не покрытое | Сценарий C (мульти-выбор), dark/light темы, zh-CN полностью | Перенесено из W2 — проверить при наличии времени |

## Вердикт

- **Все API-наблюдаемые гейты и 4 кодовых гейта зелёные** (build/jest 162-2287/lint/svelte-check),
  деплой + REST roundtrip OK, инварианты #5/#6 соблюдены, аудит — READY FOR PR (0 находок P0/P1/P2).
- **Логика покрыта Jest** новыми regression-тестами на каждый из пяти дефектов — класс багов,
  не покрытый ранее (immer-frozen мутация, relation-key mismatch, group-mode гейт, i18n-оператор,
  header-дубль), теперь зафиксирован тестами.
- **Осталось пиксельное** — 11 обязательных пунктов выше требуют глаз человека (рендер легенды,
  семантические колонки, отсутствие краша при клике, фильтрация приёмника, Escape, локали).
  REST/Jest доказали, что данные и движок готовы отрисовать это корректно, но **сам рендер и
  интеракции не наблюдаемы автоматикой** — по прямому выводу `TEST_REPORT_2026-06-26.md`
  не доверять только Jest/API после этой истории.
- **Гейты пользователя:** ручной визуальный прогон (пп. 1–11) → `git merge` ветки в main →
  `git push`. Агентами не выполняются.
